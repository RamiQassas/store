# جدول المهام (Task List)

- `[x]` تعديل وتحديث نموذج `Store` ونموذج `SubscriptionPlan` وإضافة الحقول ودالة التجديد التلقائي والخصم في `apps/stores/models.py`.
- `[x]` تفعيل `TenantManager` لعزل الموظفين والصفحات والإعدادات والفواتير في `apps/stores/models.py`.
- `[x]` إنشاء وتطبيق الترحيل (Migration) الجديد لقاعدة البيانات للحقول والخصائص المضافة.
- `[x]` تحديث `TenantMiddleware` للاستعلام حالة بحالة وفحص انتهاء الصلاحية والتجديد التلقائي.
- `[x]` تحديث النماذج في `apps/stores/forms.py` (حقل دورة الدفع وتنظيف حقل النطاق الفرعي).
- `[x]` تحديث عروض تسجيل المتجر في `apps/stores/views_admin.py` لربط المالك وتحديث الصلاحيات والدور.
- `[x]` تحديث قالب `base.html` للموقع الرئيسي لعرض شعار المتجر وألوانه واسمه عند توفره.
- `[x]` تحديث مسارات `apps/stores/urls.py` لإضافة الميزات المشتركة والأسماء البديلة للعملاء.
- `[x]` تحديث عروض التاجر وقالب الاشتراك لتفعيل تلميح وزر التجديد التلقائي.
- `[x]` إنشاء أمر الإدارة الخاص بالـ Cron Job للتجديد الدوري التلقائي.
- `[x]` التحقق النهائي بتشغيل كامل اختبارات الوحدة.
